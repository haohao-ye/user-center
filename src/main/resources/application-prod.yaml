# 连接池配置
spring:
  application:
    name: user-center
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      url: jdbc:mysql://192.168.137.1:3306/hsp_db02?rewriteBatchedStatements=true
      username: vmuser
      password: 123456
      driver-class-name: com.mysql.cj.jdbc.Driver
  session:
    timeout: 50000

# server配置
server:
  port: 8080
  servlet:
    context-path: /